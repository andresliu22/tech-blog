<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{content}}</p>
  <p>Created by {{user.first_name}} {{user.last_name}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Update Post</button>
  </p>
</div>
